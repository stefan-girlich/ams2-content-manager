import colors from '../../config/colors'
import styled from 'styled-components'

const InputLabel = styled.label`
    color: ${colors.common.textLabel};
    font-size: 0.9rem;
    text-transform: uppercase;
    margin-bottom: 8px;
`

export default InputLabel
